# Prospective information-aware diploid gate

This is a test/assessment policy change, not a search improvement or production
acceptance. The user accepts information-limited phase. The previous strict
`paired_automatic_phase_ambiguity` assertions remain unchanged as an opt-in
historical negative. Existing failures and evidence are not made green.

## Frozen acceptance

- Default far-phase test uses the **same** seed219/1536bp construction fixture,
  panel, anonymous L150/d10-per-copy/beta0.1 sample and automatic host ceilings.
  At least one complete public-valid two-copy hypothesis must match the complete
  integer vector of the assessment-only CIS/TRANS reference, local tract dosage
  and restricted45-pair minimum (absolute1e-9). Full observed support, unsupported
  positives and realized zeros are checked, with two independent joint-loss
  formulas. Checked occurrence counts add before exposure and one logarithm.
- Exact reference CIS/TRANS count equality is a demonstrated ambiguity of this
  operator. Incomplete search alone is **not** evidence that all phase is
  information-limited. Finding one compatible candidate establishes neither
  genotype uniqueness nor exhaustive support. Full-route support stays uncertified.
- Informative near-phase automatic **native-pair** control reuses the accepted
  [foundation recipe](diploid-finite-experiment.md): xorshift seed113/600bp,
  H0/H1/H2/H3 in original order, substitutions220/260, source-i groups,
  default syncmers/core37, truth H0+H3, anonymous rN reads at0,15,...450,
  alternating pooled RC. Inference receives panel/routes/sample only and
  `run(None)` with unchanged defaults, never a supplied assignment domain.
  Assessment alone enumerates10 native pairs. Require correct phased whole-DNA
  truth/query coverage1 for every retained pair, correct dosage, exact reference
  counts and restricted minimum; wrong-phase counts must differ and its objective
  must exceed correct phase by >1e-9. This is not non-native near-mosaic discovery.

Exactly two complete Assignments retain independent public validation and
within-copy physical capacity. Cross-copy same-source reuse is allowed. No
per-haploid loss sum, fitted parameter, artificial linkage or geometry/cap change.
The host remains4Mwork/8Mprofile/2048pairs/4096haploid/8192validations/128inclusive
pool/64ties/32baselines/depth4/level8/32chain/100kchainprimitives/128MiB/50kfeatures/
64segments. Finite45/10 references are restricted domains, not route optima.

## Assessment and evidence

Report actual generated/retained CIS/TRANS classes, local allele dosage, exact
injective whole-molecule and modified-copy truth **and query** denominators.
Exact-molecule coverage is not alignment coverage or QV. Actual reads/bases and
realized depth are separate from nominal d10 per copy. No phase blocks, calibrated
confidence, general sequence metric, biological capability or emission claim.

Independent assessment budgets:4096 fresh public haploid calls,8192 complete
formula evaluations counting both formulas,2M retained count cells,128MiB
conservative retained audit state,50k features,100k all-window replay reads across
both orientations. Reference45/10 formulas are separately charged90/20. Vector and
partner comparisons are non-objective work, recorded separately. These are not
allocator/RSS guarantees or hidden inference budgets.

Authoritative external Python hashlib drivers freeze complete raw inference and
support inventories **before** assessment, then verify before and after, including
failure. Assessment lives outside frozen trees. Portable tests remain external-
tool-independent for hashing: their FNV fingerprints are explicitly non-authoritative.
Sources, recipe, commands and actual executable identities are bound before runs;
old/new scientific parity uses identical existing inputs, not cross-edit executable
byte identity. All legacy regressions remain required. Default-suite count changes
are prospective acceptance changes only.

Historical negative command (configured native dependencies as usual):

```
cargo test --offline --locked --release -j4 --test test_panel_route_diploid_search paired_automatic_phase_ambiguity -- --ignored --exact --test-threads=1
```

Evidence lane: `candidate-copy-repair/diploid-information-aware-worker-v1`.
Fresh independent review found no issues and parent reproduction passed; the accepted parent evidence is identified below. Prior missing TRANS, audit overrun, interrupted/timeout runs, FNV/original executable provenance gaps and parent-v1 invalid cross-edit binary preflight remain historical failures.
No policy tuning is authorized in response to a new failed gate.

## Results — independently reviewed and parent accepted

Fresh authoritative final assessments are in
`diploid-information-aware-worker-v1/final-assessments-corrected`. Independent review found no issues, and parent reproduction accepted this prospective information-aware scope. Parent evidence is sealed under `diploid-information-aware-parent-v1` (12,300 files; manifest SHA256 `52d63d9339c17ea2e4e999fb5f9c18b9a95088d718cdcb3309eddb07310ae3fc`). This remains a test/assessment acceptance rather than a search-policy improvement.
Complete raw inference/support SHA256 inventories precede assessment; final
boundaries also inventory external source FASTA/index inputs and verify all inputs
before/after, including the expected historical failure. Five existing-input
inference trajectories match the prior parent byte-for-byte except elapsed time
and validated output-directory paths. No inference policy changed.

| Control | Objective / distinction | Actual recovery |
|---|---|---|
| Original construction | −1525.2769921785361 | exact3872bp/four molecules |
| Far observable | −1501.846720833972, equal to restricted45 minimum; CIS/TRANS vectors equal | dosage0,1,1; CIS generated/retained, TRANS absent |
| Near automatic native pair | −359.28805228858846, restricted10 minimum; wrong phase worse by45.57858931125054 and10 vector features differ | all3 retained physical descriptions spell correct1200bp/two-copy DNA pair |

Far whole-molecule truth/query exact coverage remains800/3872; modified-copy
coverage remains0/3072 for both denominators. These metrics are not made100% by
accepting observable dosage. Near whole-DNA truth/query coverage is1200/1200;
modified truth versus **all** main query is600/600 truth and600/1200 query.
Far has224 reads/33600 bases, realized8.67768595 per diploid base; near has62
reads/9300 bases, realized7.75. Nominal depth remains10 per copy.

Final portable suite: **725 passed,0 failed,27 ignored**, versus historical
723 passed,1 strict-phase failure,26 ignored. This is two new passing controls
plus moving the unchanged strict negative to opt-in, **not** an inference gain.
Focused paired:13 passed,3 ignored. Explicit strict run still fails101 at the
original missing-support assertion. All seven configured gates, B1/B2/GateA,
DNA1600, finite four controls/independent24 pairs, examples12 and Python6 pass;
protected25+2 legacy parity also passes. Independent fresh public CLI audit
checks786 paired profiles/358 haploid calls: the original761/348 plus near25/10.
The near panel, names/order, reads and sample match35 accepted input files exactly;
its graph differs only in verified fixture-root paths.

The first causal assessment failed its newly explicit conservative retained-state
cap because it eagerly retained a ledger. That attempt remains incomplete/failed.
Supervisor approved streaming one row/raw buffer at a time with unchanged limits;
fresh assessments of unchanged frozen inference pass. Four ledger passes, every
reread byte/row, fresh public calls, both formula evaluations and conservative
state are reported. The earlier spend and outputs are separate. This is logical
accounting, not an allocator bound.

Other preserved worker administrative failures: missing output parent directories
before focused/strict fixture preparation, the consequent missing-retention audit
preflight, a CLI-audit syntax preflight, and a verifier encountering a directory
named `*.json`. Fresh corrected runs pass without changing scientific inputs,
caps or recovery predicates. A pre-seal near comparison-counter overcharge of one
was corrected, and uncertified/global-bound/emission flags are now asserted
explicitly; final focused/full/authoritative assessments were rerun. Previous
historical failures remain unchanged. Nothing is staged or installed.

## Next algorithmic result

The elaborate blinded challenge proposal was held without generating challenge data. Development instead returned to the core search with one transparent deterministic mosaic-required fixture. The reviewed and parent-reproduced [joint-diploid dosage search](diploid-joint-dosage-search.md) recovers dosage `[0,1,1,2]`, the exact observable count vector and two non-native primary mosaics using a generic cross-copy physical projection operator. Unsupported long-range phase remains explicit. The next task is algorithmic scaling on a larger ordinary mosaic, not additional experiment-governance machinery.
