# Observation-compatible physical registry: diagnosis and repair proposal

## Status

**A defect is demonstrated, and a bounded enumeration/count-accounting method
passed independent review. The production repair is not implemented or approved
for a full-panel run.** No new genotype, reconstructed FASTA, QV or whole-genome
coverage result is claimed. The [original sequence controls](whole-genome-sequence-results.md)
remain the frozen comparator.

The accepted production source used by the diagnostics was
`d8a9ce6197cb08b52105c8d35e5317f2cf1e9051`. Panel, dictionary, sketch parameters,
sample collection, sample MEM-BWT, genotype parameters and original candidates
were unchanged. These are downstream, identity-targeted diagnoses of in-panel
controls—not held-out inference or a source-blind production application.

## Demonstrated mechanism

The catalog enumerates feature locations from adjacent pairs in each **stored
forward source walk**. Its reverse GBWT path reverses that same walk; it is not an
independent sketch of reverse-complement source DNA. Sample collection instead
queries both read input orientations, using the existing higher-anchor selector,
and retains noncontained overlapping MEM records.

Consequently, completeness over stored pairs is not completeness for the sample
observation operator.

### Partition986: exact frozen score contrast

A streaming diagnostic traversed all11,896,777 catalog features, retained58,533
touching10 requested groups, and retained the call/occurrence provenance of all
3,123 reached groups. It reproduced **every candidate score in those10 groups**
within4.55e−13. Unrelated factors of reached groups were not retained; this slice
is not automatically a closed joint-factor problem.

| Original modeled class | Features | Sample count sum | SK1-minus-CBM loss contribution |
|---|---:|---:|---:|
| CBM-only positive | 166 | 680 | +1892.964570 |
| CBM-only zero | 64 | 0 | −240.866667 |
| SK1-only positive | 20 | 99 | −298.565994 |
| Neither modeled | 2234 | 214 | 0 |
| **Total** | **2484** | **993** | **+1353.531909** |

There are **no shared modeled CBM/SK1 factors** in this contrast. Each modeled
feature has physical multiplicity1 in its modeled bundle; the contrast is not a
same-feature dosage advantage.

All166 CBM-only positive features have one exact anchor/spacing match in the
complete SK1 panel DNA, reverse-oriented relative to the CBM witness. Their
anchors overlap and cover the entire feature spans: unobserved interior bases
cannot explain these matches. All166 are generated by the unchanged production
collector at the omitted SK1 positions. In SK1’s stored walk,145 features have
neither expected oriented anchor and21 have only one.

Of these166 locations,137 are fully inside the **existing** SK1 candidate
`SK1#0#chrXIV:598301–603862`. The other29 cannot simply be inserted into that
interval. For example, feature11779420 at`603832–603910` crosses occurrences
380447/partition986 and380895/partition988 at boundary603862; neither contains
it completely. A location can cross catalog intervals without being outside all
intervals.

This establishes false source exclusivity in the old location table. It does
**not** establish a corrected winner by adding only these SK1 locations: complete
re-enumeration can change ownership, exclusions and exposure for every candidate.
The physical mechanisms of the other absent-SK1 error groups remain unaudited.

### Exposure is an occurrence count, not detection alone

Production replay enumerated every150bp start spanning the declared loci and
queried each transient singleton sample through the public`sample::build` and
native BWT API. Input orientations were evaluated separately and agreed.

| Physical cases | Starts per input orientation | Generated feature occurrences |
|---|---:|---:|
| 166 omitted SK1 locations | 10037 | 9717 |
| Corresponding166 CBM locations | 10037 | 9736 |
| 20 existing SK1 locations | 1271 | 1383 |

Some spanning reads contribute zero; overlapping surviving MEMs can contribute
twice at one physical locus. In particular,11779856 and11779857 each produce164
occurrences over87 starts:10 starts contribute once and77 contribute twice.
Neither binary detection nor a global depth correction represents this behavior.

## Bounded two-view test

A separately implemented diagnostic was reviewed before the parent compiled and
ran it. It independently calls the orientation-specific native iterator and
frozen dictionary lookup. Calling the public higher-anchor matcher twice would
not expose two independent raw views.

The diagnostic searches **all exact feature-DNA occurrences on every supplied
complete source path**, pairs anchors within each raw view separately, and then
unions physical locations. It never interleaves anchor streams. It also compares
cropped raw views with full-source restrictions and compares coordinate-tagged
MEM contributions with public singleton BWT counts.

| Measured item | Result |
|---|---:|
| Frozen anchor-covered features | 250 |
| Complete supplied source paths / bp | 2 / 827384 |
| Physical candidates, all native-generatable | 420 |
| Locations recovered beyond stored enumeration | 170 |
| Generatable locations missing the two-view **union** | 0 |
| Declared candidate/start/orientation cases | 50188 |
| Main singleton executions after exact reuse | 24078 |
| Additional constructed synthetic singleton executions | 8 |
| Main crop/count/record/coordinate failures | 0 |
| Synthetic raw crop comparisons / mismatches | 2772 / 0 |
| Frozen regression assertions passed | 3 / 3 |
| Diagnostic unit tests passed | 11 / 11 |

The170 recovered locations include166 positive and four zero-count features.
The two views are not each individually complete. The constructed two-copy
fixture preserves physical spans`[72,150)` and`[285,363)`, each contributing one
against public count two; it is not evidence of actual sample placements.

Native execution took74.47s and1,889,888KiB peak RSS, using affinity252–255,
nice+10 and at most four build jobs. This is a diagnostic cost, not a scalable
registry benchmark. The test covers two paths, anchor-covered features, the
specified error-free contexts and lengths—not all panel contexts, noisy reads,
gapped feature interiors or novel mosaic junctions.

A separate Python check re-encoded adjacent signed-node/gap pairs from saved
surviving synthetic records. All per-coordinate multiplicities and all
**6,021,500 public-count comparisons across24,086 audits** agreed. This is an
independent check of saved accounting, not a second execution of collection or
BWT construction. The final reviewer accepted the bounded evidence with these
limits, finding no remaining issues.

## Proposed implementation, in order

### Execution model: iterate partitions, not alternative pangenome views

The partition is the unit of iteration. Each physical genome piece has one
partition owner and one member representation in that partition's pangenome
view. Keep that ownership fixed. The orientation-dependent observation defect
must be reconciled **inside the partition**, not represented as two copies, two
panels or two independently called partition views.

The source-first two-view experiment above was a diagnostic oracle, not the
intended production execution plan. Raw orientation traces are an internal
compatibility detail of the current observer. They feed one physical incidence
record per feature/source coordinate; they are not additional member paths.

The exact-once audit confirms this invariant on the frozen input: **19,421
partitions, 383,849 member intervals and all 9,901 source paths / 3,336,976,856 bp**.
There are zero uncovered bases, zero multiple-partition-owned bases and zero
overlapping duplicate descriptions. The sum of member lengths equals the total
source length. This is stronger than the earlier union-coverage check.

Read-only context halos do not own bases, add copies, receive independent
genotype calls or emit sequence twice.

### 1. Share the observation primitives without changing the sample

Expose/reuse orientation-specific raw extraction separately from the existing
higher-anchor wrapper. Factor the current coordinate-tagged collection and
content-subwalk pruning so that sample construction still discards coordinates
and emits exactly the same canonical weighted records.

Require unchanged sample payloads/counts on regression inputs, including RC,
ambiguity, repeated features and overlapping MEMs. Coordinates used to compile
expected contributions are transient coordinates of synthetic panel contexts;
no actual-read IDs, placements or read-by-haplotype table are introduced.

### 2. Reconcile observations inside each partition

Keep the existing feature-token universe and IDs initially. Iterate partitions
in their existing deterministic order, fetching each member's owned sequence
and the original-source context required by the observation operator. Apply the
same procedure to every member, without identity-specific branches. Retain one
physical member representation with its full source coordinates and orientation.

For the current observer, independently extracted raw orientation traces can
supply the missing feature incidences. Find pairs within each trace separately,
normalize to source-forward coordinates, and emit one record keyed by
`(feature_id, full_source_path, start, end)`. Do not construct an interleaved
walk or expose the traces as parallel pangenome views. Context-halo discoveries
must be routed to their actual owner or a boundary record, never counted twice.

Preserve distinct physical spans even when their DNA is identical. Do not insert
only the diagnosed SK1 locations, add dictionary words, concatenate observations,
modify source intervals or silently change the sketch scheme.

Two-view enumeration is a candidate fast path, not an unconditional completeness
theorem. Its sufficient conditions are: cropped raw anchors equal the relevant
full-view restriction; dictionary filtering is identical; and retained MEMs are
contiguous subwalks of the selected read walk. Then an emitted adjacent pair is
adjacent in one of those source views. Expand property/oracle tests to additional
lengths, ambiguity masks, source ends and features with uncovered interiors.
An observed counterexample must fail the capability gate and trigger an exact
context-aware fallback/design revision—not an interleaved-anchor workaround.

Initial capability is **error-free source replay**. Sequencing errors can erase
intervening anchors or change anchor words, so this gate does not establish a
complete prediction model for noisy reads. The capability and limitations must
be explicit in versioned artifacts.

### 3. Preserve partition ownership; recompute feature support

The base-to-partition ownership map does not change. Against that fixed map,
derive full-span containment, crossing and shared-partition support for each
reconciled physical feature incidence. A formerly local **observation factor**
may turn out to span or occur in several partitions; that is not overlapping
ownership of the underlying genome pieces. Do not assign a crossing factor by
its start alone or expand an interval to retain a useful factor.

A token feature can recur in different physical pieces even when every base has
exactly one owner. Use a compact shared/boundary-factor ledger to combine their
selected-copy contributions inside one rate and count the sample observation
once. This is cross-partition bookkeeping, not a second pangenome view. Keep
unsupported/nonlocal observations and their exclusions explicit until the
consumer can handle their complete scope.

The two-path diagnostic does not close that global factor ledger. Complete
incidence accounting must cover every partition. BED groups remain ownership
descriptions, not certified homologous loci.

The exposure-support context also matters. Full-source flanks cannot silently be
assumed across a new mosaic junction. The initial local consumer must either
validate the required context or classify the factor as context-dependent and
unresolved/nonlocal, retaining its observation and exclusion reason. This is a
versioned factor-validity change, not frequency masking or a zero-copy call.
Any additional exclusions must be measured against complete genome denominators.

### 4. Compile occurrence-weighted expected contributions

For physical occurrence`o`, let`q(f,o,s,L,theta)` count surviving MEM-substring
contributions at that coordinate from a length-`L` read starting at`s`, under
input orientation`theta`. Preserve values0,1,2,…, including repeated contributions
at one coordinate. Under the declared error-free uniform-start working model:

\[
e_{fo}=\frac{\sum_L n_L\sum_s\mathbb E_\theta[q(f,o,s,L,\theta)]}
                 {\sum_L n_L L},\qquad
\mu_f=\beta+d\sum_o z_o e_{fo}.
\]

Average orientation contributions; do not add them as extra reads. Respect source
ends and valid start bounds. When one read spans multiple copies, assign each
surviving substring to its own synthetic coordinates; never give the read’s total
count to every copy. Sum physical-copy contributions **inside one rate and one
factor loss**.

The old span exposure is recovered when each eligible start contributes exactly
once. A zero-exposure location is not evidence of deletion. Source-context
profiles do not calibrate real coverage, sequencing error, independence between
features, posterior confidence or junction validity.

### 5. Bound memory and prove any execution shortcuts

Do not make another fully materialized41GB JSON object a prerequisite. A proposed
partition-major compiler can stream existing feature definitions into a compact
exact lookup, process one partition at a time, and externally sort/merge bounded
runs of physical incidence records for the shared/boundary ledger. Fetch member
cores and validated context rather than materializing the full pangenome. Any
boundary stitching of panel traces must follow actual source continuity; it is
not permission to concatenate sample MEMs. Deduplication must be deterministic
and retain complete ownership and orientation provenance. Record input/model fingerprints, completed sources
and checksums for resumable immutable stages; never append to a mismatched run.

The singleton-FASTA oracle is not the production exposure algorithm. First keep
an exact in-memory reference implementation. Only then test reuse of identical
**both-view node/relative-coordinate contexts**, with panel/model/read-length
identity in the cache key, and weighted reuse of consecutive starts proven to
produce identical records. Keep both views and their relative alignment; do not
cache merely by target feature or donor label. Count totals should accumulate as
checked integers before normalization. Cache limits may cause recomputation,
never sampling, truncation or dropped copies.

Profile time, peak memory, scratch space, physical-record counts and exact output
equality before a full-panel run. If source chunking is needed, demonstrate
boundary/crop equivalence with retained halo/ownership rules or use a validated
fallback. No all-panel runtime or memory improvement is established here.

### 6. Integrate as an explicit new model, then evaluate sequence

Use versioned registry/exposure/validity identifiers and reject incompatible
artifacts. Preserve the current scores and old whole-template ranking verbatim;
new locations/exposure/ownership change the objective and require newly labelled
scores. The unchanged sample MEM-BWT may be reused only if its panel and count
policy remain compatible. Reclassification of old scores alone cannot perform
this repair.

Keep depth10, background0.1, existing deviance cutoff and switch penalty fixed in
the first controls. Re-score, rethread and reconstruct afresh. Add the separately
diagnosed background-supported eligibility rule consistently to classification,
threading and reconstruction validation; background preference means unsupported
source evidence, not deletion.

Only after support/ownership are checked should repeated or off-axis bundles gain
new copy-conserving unphased reconstruction. Do not emit one copy per sketch view
or per reference-axis appearance, resolve tied physical multisets arbitrarily,
force the globally identified donor, or impute an unvalidated recombinant join.

## Acceptance gates for that proposal

1. Legacy sample records/counts unchanged by primitive refactoring; old artifacts
   remain explicitly interpretable under their original model.
2. Exact synthetic observation and physical-copy conservation, including zero,
   repeated-MEM and multi-copy contributions; parity with the public oracle.
3. Expanded crop/location tests and an explicit supported capability/error model;
   exact-once physical ownership and complete incidence/exclusion accounting,
   without changing the chosen partitions.
4. Bounded, deterministic, restart-compatible resource behavior established before
   authorizing the full-panel compiler.
5. Fresh unchanged S288C/SK1 controls: report errors, conditional alignment QV,
   complete truth/query coverage, unresolved sequence and extra/copy ambiguity.
   Higher conditional QV obtained only by abstention is not complete recovery.
6. Reproduce the complete-template special case under its separately labelled
   objective without restricting ordinary mosaic inference to one donor.

A useful downstream test invariant follows conditionally: for an error-free
in-panel source and a complete observation-compatible registry, any positive
**uniquely owned** feature must have a generating source occurrence in its owner.
A group with no such source bundle cannot retain that source’s positive feature
as exclusive local evidence. Use this to detect incomplete registry/ownership,
not to seed candidates from truth. It does not prove correct sequence placement,
allelic homology or complete reconstructed coverage.

Strict held-outs still require the predeclared whole-identity/alias exclusions and
fresh panel/catalog/sample construction. Recombinant, noisy and coupled-ploidy
gates remain separate and unpassed.

## Reproducibility and retained failures

Root`B`:
`/home/erikg/impg/target/experiments/genome-mem-bwt-pipeline/candidate-copy-repair/`.

- `feature-slice-v2/`: streamed features, complete reached occurrence provenance
  and score checks; `feature-probe/` contains native diagnostic sources.
- `witnesses-v1/`, `native-witnesses-v2/`, `native-witness-summary.json`: frozen
  witness selection, DNA searches and production singleton replay.
- `diagnosis-provenance-v1/manifest.json`: source snapshot and SHA256s, including a
  fresh verification of the complete41,129,985,798-byte catalog against
  `3fa9f97c1110f2bfd42ebcaa358ca9fb1ba056fbcf55b538e91f376e62d19117`.
  This is separate from the streaming helper’s checksum-header comparison.
- `dual-view-probe/`, `dual-view-reviewed-source/`, `dual-view-run-v1/`: reviewed
  code, parent build inputs, commands,11 tests, native results and resource logs.
- `dual-view-run-v1/parent-verification-v2/`: complete JSONL accounting, current
  panel fingerprints matching native-start fingerprints, SHA256s and frozen
  executable. Binary SHA256:
  `5ec6b2fb1b7dd84cd881317568a45a4fc6319107f496cbb28042fef657869e5b`.
- `dual-view-run-v1/independent-record-accounting.json` and
  `check-tagged-record-counts.py`: separate numerical accounting check.
- `partition-ownership-exact-once.json` and `audit-partition-ownership.py`: full
  coordinate sweep over all frozen source occurrences, checking gaps, distinct
  partition ownership and duplicate descriptions separately.
- `reviews/`: retained implementation, static review and diagnosis reports.

Failed diagnostic attempts were retained: missing inherited Cargo patches,
incorrect catalog-envelope traversal, a Rust string/path API mismatch, and an
uppercase comparison against lowercase native DNA. Before the accepted dual-view
run, the parent corrected a vacuous fixture group`986` to`partition986`; no Rust
source changed after review. A later parent verifier confused2,772 orientation
comparisons with JSONL rows; the correct count is1,386 rows containing two views.
Its failure was retained; the native run was not repeated or altered.

The standalone probes are experimental artifacts, not installed production
commands. No production source or original inference output changed during these
diagnostics. The normal installed executable remained the previously accepted
sequence-stage build.
